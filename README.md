# `Ventana`

## An iterator-based windowing library built in Rust

A key feature for this library is extensibility. Users can implement their own backends to replace the ones built into the core library.
